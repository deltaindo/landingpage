# Production Environment - Update this with your actual backend URL
# Option 1: If backend is on separate domain
NEXT_PUBLIC_API_URL=https://api.deltaindo.co.id/api

# Option 2: If backend is on same domain under /api path
# NEXT_PUBLIC_API_URL=/api

# Option 3: If backend is on different domain
# NEXT_PUBLIC_API_URL=https://your-backend-domain.com/api
